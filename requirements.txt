# LLM Benchmarking Framework — Python dependencies
# Pinned to versions known to work on GitHub Actions (ubuntu-latest, Python 3.11)

# ── Core ML / HuggingFace stack ──────────────────────────────────────────────
torch==2.2.2
transformers==4.40.1
tokenizers==0.19.1
accelerate==0.30.1
huggingface-hub==0.23.0
safetensors==0.4.3

# ── System / process metrics ─────────────────────────────────────────────────
psutil==5.9.8

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm==4.66.4
numpy==1.26.4
packaging==24.0
filelock==3.14.0

# ── Optional: sentence evaluation (used for future semantic scoring) ─────────
# sentence-transformers==2.7.0

# ── Notes ────────────────────────────────────────────────────────────────────
# Large models (Phi-2, Mistral-7B, LLaMA-3) should be run locally and
# results pushed to the repo. GitHub Actions only runs distilgpt2 / gpt2
# by default due to memory / time constraints.
